package com.example.banqueapplication;

public interface DateValidator { // Interface that we gonna implements to check if the user respected the date format or not
    boolean isValid(String dateStr);
}
